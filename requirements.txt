# ManualMan — pip install -r requirements.txt
# Python 3.11+ required

# ── Web UI ────────────────────────────────────────────────────────────────────
streamlit>=1.35.0

# ── Environment / config ──────────────────────────────────────────────────────
python-dotenv>=1.0.0

# ── LlamaIndex core + integrations ────────────────────────────────────────────
llama-index-core>=0.11.0
llama-index-embeddings-voyageai>=0.2.0
llama-index-vector-stores-chroma>=0.2.0
llama-index-llms-anthropic>=0.3.0
llama-index-readers-file>=0.2.0

# ── Vector database ───────────────────────────────────────────────────────────
chromadb>=0.5.0

# ── Hybrid BM25 retrieval ─────────────────────────────────────────────────────
rank-bm25>=0.2.2

# ── Cross-encoder reranking (optional — downloaded lazily on first use) ───────
sentence-transformers>=3.0.0

# ── PDF parsing + image extraction ───────────────────────────────────────────
PyMuPDF>=1.24.0
pdfplumber>=0.11.0

# ── AI APIs ──────────────────────────────────────────────────────────────────
anthropic>=0.28.0
voyageai>=0.3.0

# ── Image handling ────────────────────────────────────────────────────────────
Pillow>=10.0.0

# ── Data handling ────────────────────────────────────────────────────────────
pandas>=2.0.0

# ── Numerics (entity unit parsing) ───────────────────────────────────────────
numpy>=1.26.0
